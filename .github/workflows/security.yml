name: Security (reuses central config)
on: [push, pull_request]
permissions:
  contents: read
  security-events: write
jobs:
  security:
    uses: ThompsonCloud/ai-sdlc-security-config/.github/workflows/security-review.yml@main
